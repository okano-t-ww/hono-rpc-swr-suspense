{
	"name": "hono-rpc-swr-example",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter client dev & pnpm --filter server dev",
		"build": "pnpm -r build",
		"typecheck": "pnpm -r typecheck",
		"check": "biome check --write"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"typescript": "^5.7.0"
	}
}
